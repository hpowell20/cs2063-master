TOTAL: 20 marks

GeoDataListActivity:
  - Create a variable used as a reference for the progress bar to use in
      the AsyncTask class (1 mark)
  - Create an Intent to start GeoDataDetailActivity (1 mark)
  - Add the extras to the intent (1 mark)
  - downloadGeoData method
    - Check the network connection (1 mark)
    - Show toast if not connected (1 mark)
  - DownLoaderTask:
    - Get a reference to the progress bar (1 mark)
    - onPreExecute() Disable the button so it can't be clicked again
         once a download has been started (1 mark)
    - onPreExecute() Set the progress bar's maximum to be DOWNLOAD_TIME, its
         initial progress to be 0, and also make sure it's visible (3 mark)
    - doInBackground() Create an instance of JsonUtils and get the data from
         it (1 mark)
    - doInBackground() Store the data in mGeoDataList (1 mark)
    - doInBackground() Update the progress bar; calculate an appropriate
         value for the new progress using i (1 mark)
    - onPostExecute() Enable the button again (1 mark)
    - onPostExecute() Reset the progress bar, and make it disappear (2 marks)
    - onPostExecute() Setup the RecyclerView (1 mark)
    - onPostExecute() Create a Toast indicating that the download is complete (1 mark)
    - onProgressUpdate() Update the progress bar using values (1 mark)

JsonUtils:
  - Establish an HttpURLConnecion to REQUEST_URL (1 mark)
